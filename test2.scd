s.boot
(
{
	var sig;
	sig - SinOsc.ar(200, 0, 8.2);
}.play
)





(
~sound = {
	var sig;
	sig = PinkNoise.ar *0.2;
	};
)

~sound.value;s.boot
(
{
	var sig;
	sig - SinOsc.ar(200, 0, 8.2);
}.play
)


(
(
((
	(
		(
			(
			)
		)
	)
))
)
)

	max(4,5)